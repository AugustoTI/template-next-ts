commit-msg:
  commands:
    'lint commit message':
      run: pnpm commitlint --edit {1}

pre-commit:
  parallel: true
  commands:
    'running-eslint':
      run: 'pnpm lint src'
    'running-tests':
      run: 'pnpm test -- --detectOpenHandles'
